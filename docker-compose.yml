services:
  ean13-api:
    image: ghcr.io/springsunx/ean13-api:latest
    container_name: ean13-api
    network_mode: bridge
    restart: always
    ports:
      - 8080:8080
    environment:
      - PORT=8080